import { atom } from 'recoil';

export const countState = atom({
  key: 'contState',
  default: 30,
});

export const RedcountState = atom({
  key: 'RedcountState',
  default: 30,
});

export const GreencountState = atom({
  key: 'GreencountState',
  default: 30,
});

export const BluecountState = atom({
  key: 'BluecountState',
  default: 30,
});